#!/bin/bash
set -e

images=(
    debian:jessie
    icloudway/php:5.6
    mysql:5.5
    icloudway/redis:3.2
)

if ! docker inspect "${images[@]}" &> /dev/null; then
    for img in "${images[@]}"; do
        docker pull "$img"
    done
fi
